# --- Godot 4+ ---
# Папка, в которой Godot хранит кеш импортированных ассетов и метаданные.
# Эту папку ОБЯЗАТЕЛЬНО нужно игнорировать. Она генерируется автоматически
# для каждого пользователя и будет вызывать постоянные конфликты.
.godot/

# --- Godot 3.x (на всякий случай, если кто-то откроет проект в старой версии) ---
.import/

# --- Файлы экспорта / Сборки проекта ---
# Никогда не храните скомпилированную игру в репозитории.
builds/
export/
*.pck
*.exe
*.dmg
*.app
*.html
*.wasm

# --- Конфигурационные файлы, специфичные для пользователя ---
# Файл с пресетами экспорта часто содержит личные пути и ключи (например, Android Keystore).
# Лучше иметь шаблон (export_presets.cfg.template), а сам файл игнорировать.
export_presets.cfg

# Файл для переопределения настроек проекта локально.
override.cfg

# --- Локальная история скриптов редактора Godot ---
.history/

# --- Сгенерированные файлы ---
# Скомпилированные файлы переводов. В репозитории хранятся исходники (*.po, *.csv).
*.translation

# --- Системные файлы macOS, Windows, Linux ---
.DS_Store
Thumbs.db
.Trash-*
*~
*.swp